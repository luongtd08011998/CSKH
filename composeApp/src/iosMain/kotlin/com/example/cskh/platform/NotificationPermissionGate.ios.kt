package com.example.cskh.platform

import androidx.compose.runtime.Composable

@Composable
actual fun NotificationPermissionGate() {
    // iOS permission flow is handled separately (no-op for now).
}

